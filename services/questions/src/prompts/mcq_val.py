# from langchain_core.prompts import ChatPromptTemplate

# validator_system_prompt = """
# Вы — ассистент по проверке вопросов (MCQ-validation assistant). Оцените вопрос с четырьмя вариантами ответов (MCQ), используя только информацию, предоставленную в блоке "Исходная информация" (Source Information).

# Проверьте вопрос по следующему чек-листу (Checklist):
# 1. GRAMMAR_FLUENCY (Грамматика и понятность)
# Написан ли вопрос грамматически корректно и ясно? Содержит ли он законченную мысль?
# 2. SINGLE_CORRECT_KEY (Один правильный ключ)
# Является ли правильным строго один вариант?
# 3. OPTION_UNIQUENESS (Уникальность вариантов)
# Все ли 4 варианта уникальны и не содержат дубликатов (или синонимов, которые могут сделать вопрос неоднозначным)?
# 4. ANSWERABLE_FROM_SOURCE (Ответ вытекает из источника)
# Вытекает ли указанный правильный ответ ИСКЛЮЧИТЕЛЬНО из Исходной информации без привлечения внешних знаний?
# 5. DISTRACTOR_QUALITY (Качество дистракторов)
# Являются ли неверные варианты правдоподобными, но при этом гарантированно ошибочными в рамках заданного контекста?

# Если есть малейшие сомнение в двусмысленности, или если правильный ответ опирается на внешние знания — отклоняйте вопрос (is_approved=False) и предоставляйте четкий feedback.

# === FEW-SHOT ПРИМЕРЫ (ПРАВИЛА ОТБРАКОВКИ) ===
# Пример Отклонения (Нарушение OPTION_UNIQUENESS):
# Вопрос: Какой город является столицей США?
# Варианты: A) Нью-Йорк, B) Вашингтон (округ Колумбия), C) D.C., D) Бостон.
# Вердикт: Отклонить (is_approved=False). Причина: Варианты B и C означают одно и то же. Нарушена уникальность.

# Пример Отклонения (Нарушение SINGLE_CORRECT_KEY / Черный список):
# Контекст: Изучение простых чисел. Черный список: 3, 5, 7.
# Вопрос: Какое из чисел является простым?
# Варианты: A) 2, B) 4, C) 5, D) 8.
# Вердикт: Отклонить (is_approved=False). Причина: Вариант C (5) находится в черном списке и тоже является правильным ответом. У вопроса два правильных ключа (A и C).
# ============================================

# СТРОГО СЛЕДУЙТЕ ЭТОМУ ФОРМАТУ ВЫВОДА (JSON):
# {format_instructions}
# """

# validator_user_prompt = """
# Следуйте инструкциям в системном промпте, чтобы оценить следующий MCQ, основываясь только на Исходной Информации.

# Оцениваемый вопрос:
# Вопрос: "{question}"
# Изображение (если есть): "{image_url}"
# A) {option_a}
# B) {option_b}
# C) {option_c}
# D) {option_d}
# Заявленный правильный ответ: "{correct_key}"
# Обоснование автора: "{justification}"

# Исходная информация:
# "{path_context}"

# ЧЕРНЫЙ СПИСОК (Запрещенные альтернативные правильные ответы):
# "{forbidden_distractors}"

# ЗАДАЧА:
# Проведите анализ по чек-листу. Если хотя бы один из дистракторов совпадает или является синонимом терминов из "ЧЕРНОГО СПИСКА", это нарушает правило SINGLE_CORRECT_KEY — немедленно бракуйте вопрос (is_approved=False, is_single_correct=False).
# """

# validator_prompt_template = ChatPromptTemplate.from_messages(
#     [("system", validator_system_prompt), ("user", validator_user_prompt)]
# )

from langchain_core.prompts import ChatPromptTemplate

validator_system_prompt = """
Ты — строгий и беспристрастный редактор образовательных тестов.
Твоя задача — проверить сгенерированный вопрос (MCQ) на соответствие исходным фактам и стандартам качества.

Ты должен проверить вопрос по 5 критериям:
1. Достоверность фактам (Answerable from context)
2. Наличие строго одного верного ответа (Single correct key)
3. Качество неверных вариантов (Distractor quality)
4. Понятность формулировки вопроса (Clear stem)
5. Грамматика (Grammar)

БУДЬ БЕЗЖАЛОСТЕН. Если есть малейшее сомнение в двусмысленности или если правильный ответ опирается на внешние знания, которых нет в контексте — отклоняй вопрос (is_approved = False) и пиши четкий feedback для исправления.
Дополнительное правило проверки: Если в ИСХОДНЫХ ФАКТАХ присутствуют пояснения "(Info: ...)", убедись, что Генератор не исказил их смысл при формулировании вопроса.

СТРОГО СЛЕДУЙ ЭТОМУ ФОРМАТУ ВЫВОДА (JSON):
{format_instructions}
"""

validator_user_prompt = """ИСХОДНЫЕ ФАКТЫ (Граф знаний):
{path_context}

ИЗВЕСТНЫЕ АЛЬТЕРНАТИВНЫЕ ПРАВИЛЬНЫЕ ОТВЕТЫ (Их не должно быть в дистракторах!):
{forbidden_distractors}

СГЕНЕРИРОВАННЫЙ ВОПРОС НА ПРОВЕРКУ:
Вопрос: {question}
Ссылка на изображение: {image_url}
A) {option_a}
B) {option_b}
C) {option_c}
D) {option_d}
Заявленный правильный ответ: {correct_key}
Обоснование автора: {justification}

Проведи анализ. Если хотя бы один из дистракторов совпадает или является синонимом терминов из "ИЗВЕСТНЫХ АЛЬТЕРНАТИВНЫХ ОТВЕТОВ" — немедленно бракуй вопрос (is_approved=False, is_single_correct=False).
"""

validator_prompt_template = ChatPromptTemplate.from_messages(
    [("system", validator_system_prompt), ("user", validator_user_prompt)]
)
